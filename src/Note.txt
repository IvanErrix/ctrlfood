Fare mockup
ombra nelle label in cassa
Fare controlli nelle procedure

SwingWorker<Boolean, Integer> worker = new SwingWorker<Boolean, Integer>() {
			  @Override
			  protected Boolean doInBackground() {
			  }
			    return true;
			  }
			};
			worker.execute();
			
			
Bug in aggiungi al carrello quando ci sono due prodotti in negozio con lo stesso nome nella label id non carica i due diversi id